services:
  - type: web
    name: bela-cafe
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview -- --host 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    healthCheckPath: / 