services:
  - type: web
    name: bela-cafe-static
    env: static
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production
    plan: free 